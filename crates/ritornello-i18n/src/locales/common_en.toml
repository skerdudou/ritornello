play = "Play"
pause = "Pause"
stop = "Stop"
error = "Error"
# Loading a plugin's UI (the SPA shell, web/app/src/views/PluginView.ts).
# These keys live in the `common` layer — inherited by EVERY catalog —
# and not in the core's: the shell resolves first against the plugin's
# catalog, which is empty precisely when the plugin is unreachable.
loading = "Loading…"
plugin_unavailable = "Plugin interface unavailable."
plugin_unavailable_cause = "Plugin interface unavailable: {cause}"
plugin_contract_mismatch = "This plugin's interface must be rebuilt: it does not match the current UI contract."
