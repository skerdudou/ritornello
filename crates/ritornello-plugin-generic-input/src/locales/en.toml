# validation messages
duplicate_code = "code {code} is bound twice on {device}"
select_out_of_range = "preset {n} out of range 0-9 on {device}"
unknown_command = "unknown command bound to code {code} on {device}"
unknown_preset = "unknown preset: {preset}"
bad_request = "invalid request: {detail}"
save_failed = "the save failed"
# live validation, client side: the same conflict as `duplicate_code`,
# but detected before the round trip to the server.
conflict_code = "code {code} is already assigned to {action}"
conflict_dup = "code {code} is entered twice"
save_conflicts = "Resolve the duplicate key codes before saving"

# admin page
admin_title = "input bindings"
device_label = "Device"
btn_refresh = "Refresh"
col_action = "Action"
col_code = "Key code(s)"
btn_learn = "Learn"
btn_clear = "Clear"
preset_label = "Preset"
btn_load_preset = "Load"
btn_import = "Import…"
btn_export = "Export"
btn_save = "Save"
btn_cancel = "Cancel"
dlg_learn_title = "Key learning"
dlg_learn_desc = "Press a key on the device “{device}”…"
learn_append_label = "Add to the existing codes instead of replacing them"
learn_timeout = "No key detected — cancelled"
learn_countdown = "{s} s left"
saved = "Saved ✓"
save_error = "Error: {cause}"
load_error = "Loading error: {cause}"
no_device = "No input device detected"

# the 23 actions
act_select_1 = "Preset 1"
act_select_2 = "Preset 2"
act_select_3 = "Preset 3"
act_select_4 = "Preset 4"
act_select_5 = "Preset 5"
act_select_6 = "Preset 6"
act_select_7 = "Preset 7"
act_select_8 = "Preset 8"
act_select_9 = "Preset 9"
act_select_0 = "Key 0"
# "Presets" rather than a bare "+10": this key shifts the next digit by ten
# (+10 then 4 selects station 14), it does not move playback. Without that
# word it reads as "+10 seconds" as soon as the seek actions share the same
# list — which is exactly
# what happened.
act_plus10 = "Presets +10"
act_volume_up = "Volume +"
act_volume_down = "Volume -"
act_mute = "Mute"
act_play_pause = "Play/pause"
act_stop = "Stop"
# "in track" tells them apart from "Previous"/"Next", which change track.
# No number in the label: the step is adjustable from the configuration page,
# and "10 s" would become a lie.
act_seek_back = "Rewind in track"
act_seek_forward = "Fast forward in track"
act_next = "Next"
act_prev = "Previous"
act_eject = "Eject"
act_source_cycle = "Change source"
act_toggle_random = "Shuffle"
act_toggle_repeat_all = "Repeat all"
act_power = "Standby"

# source shortcut rows, appended at the end of the table (one per source the
# core announces via `/api/presets`, plus one for a source still bound but
# no longer installed)
act_select_source = "Source: {source}"
act_select_source_unknown = "Source: {source} (not installed)"
