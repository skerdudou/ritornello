[package]
name = "ritornello-plugin-console"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "ritornello-plugin-console"
path = "src/main.rs"

[dependencies]
anyhow = "1"
# For `localtime_r`: the local time of the standby screen. Already the repository's
# idiom for what the C library can do on its own (see core/system.rs).
libc = "0.2"
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
async-trait = "0.1"
ritornello-proto = { path = "../ritornello-proto" }
ritornello-plugin-sdk = { path = "../ritornello-plugin-sdk" }

[dev-dependencies]
tempfile = "3"
